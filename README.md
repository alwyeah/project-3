# About Us HTML Page

This project contains a simple **Company About Us web page** built using **HTML5**.  
It is designed as a beginner-friendly example demonstrating the basic structure and commonly used HTML tags.

---

## 📄 Project Description

The About Us page presents information about a company, including its mission, values, and contact links.  
The page is fully static and can be opened in any modern web browser.

---

## ✨ Features

- Uses semantic HTML5 structure
- Headings from `<h1>` to `<h3>`
- Paragraph content using `<p>`
- Text emphasis using `<strong>` and `<em>`
- Internal and external hyperlinks using `<a>`
- Line breaks using `<br>`
- Section separation using `<hr>`

---




